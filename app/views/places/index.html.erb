<% @places.each do |place| %>
<%= will_paginate @places, previous_label: h("<"), next_label: h(">") %>

<!-- or custom pagination -->


  <div class="booyah-box col-10 offset-1 will_paginate">
    <h1><%= place.name %></h1><br />
    <i><%= place.address %></i><br />
    <p><%= place.description %></p><br />
  </div>

<% end %>